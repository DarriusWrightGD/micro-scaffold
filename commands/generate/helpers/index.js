const formatOutputDirectory = require('./formatOutputDirectory');
const generateTemplateTypes = require('./generateTemplateTypes');

module.exports = {
  formatOutputDirectory,
  generateTemplateTypes
}